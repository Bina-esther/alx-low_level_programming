/**
* Function definition
*/
#include "main.h"
/**
* Function name: _islower
* Purpose: checks if a character is lowercase
*/
int _islower( int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
