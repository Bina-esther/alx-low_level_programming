#include <stdio.h>
/**
* Task 1
* Program- prints the number of arguments passed into it followed by a new line
*/
int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc - 1);

	return (0);
}
